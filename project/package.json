{
  "name": "docker-wp-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsc && vite build --mode development",
    "build": "tsc && vite build --mode production",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "sass": "^1.83.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
