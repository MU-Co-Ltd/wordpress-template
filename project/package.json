{
  "name": "docker-wp-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsc && vite build --mode development",
    "build": "tsc && vite build --mode production",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.66.1",
    "typescript": "^5.2.2",
    "vite": "^4.4.9"
  }
}
