{
  "name": "docker-wp-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsc && vite build --mode development",
    "build": "tsc && vite build --mode production",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.79.3",
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
